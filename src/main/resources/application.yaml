server:
  port: 8081

spring:
  # spring 基础配置
  application:
    name: test-station
  # 数据库配置
  datasource:
    username: root
    password: 1314520ASD@
    url: jdbc:mysql://localhost:3306/test_station?serverTimezone=UTC&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 配置日志输出 使用默认控制台打印
  mapper-locations: classpath:mapper/*.xml
